using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("친구가 수정해서 다시 올림");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
